package com.softeng2red.dungeon.framework;

import com.softeng2red.dungeon.objects.Game_Timer;
import com.softeng2red.dungeon.objects.HUD;
import com.softeng2red.dungeon.objects.Health;
import com.softeng2red.dungeon.objects.Player;
import com.softeng2red.dungeon.window.Camera;
import com.softeng2red.dungeon.window.Game;
import com.softeng2red.dungeon.window.Handler;
import org.junit.Test;

import static org.junit.Assert.*;

public class KeyInputTest {

    @Test
    public void playerMovementTest() {
        // if this test passes, the player moves when pressed left keyboard or right keyboard

        // create handler object and player object
//        Game_Timer game_timer = new Game_Timer(0,0, ObjectId.Game_Timer);
//        Camera cam = new Camera(0,0);
//        Handler handler = new Handler(cam, game_timer);
//        handler.addObject(new Player(100,100, handler, cam, ObjectId.Player));

//        Game game = new Game();
//        game.init();

//        KeyInput keyInput;
//        Health health;
//        for(int i=0; i<game.handler.object.size(); i++) {
//            GameObject tempObject = game.handler.object.get(i);
//            if(tempObject.getId() == ObjectId.KeyInput) {
//                tempObject.;
//            }
//        }
//        keyInput.


    }

}